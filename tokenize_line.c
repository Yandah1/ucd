#include "maonty.h"

/**
 * tokenize_line - function to tokenize line from files
 *
 * Return: nothing
 */

void tokenize_line(voisd)
{
	int i = 0;
	char *dels = " \n", *token = NULL, linecpy = NULL;

	linecpy = malloc(sizeof(char) * (strlen(global_fd->line) + 1));
	strpcy 


